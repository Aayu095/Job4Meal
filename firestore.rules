rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // DEMO MODE: Allow all reads and writes for hackathon demo
    // For production, implement proper authentication checks
    match /{document=**} {
      allow read, write: if true;
    }
  }
}